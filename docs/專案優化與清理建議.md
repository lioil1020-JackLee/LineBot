# 專案優化與清理建議

最後更新：2026-04-07

## 本次已完成

1. 清理本機產物資料夾：`MagicMock/`。
2. 清理快取資料夾：`.pytest_cache/`、`.ruff_cache/`。
3. 更新 `.gitignore`，避免再次誤加入本機產物與 IDE/工具快取。
4. 修正 `scripts/build.ps1` 的專案根路徑推導錯誤（避免找不到 spec 檔）。
5. 重寫 `README.md`，同步當前功能與實際操作流程。
6. 更新 `docs/打包指南.md` 與 `docs/LM_Studio_自動啟動.md`，修正過時資訊。

## 目前品質狀態

1. 單元測試：`51 passed`。
2. 專案可正常啟動與運作（依當前設定）。
3. 仍有 Ruff 檢查項目可進一步整理（主要是 import 排序與長行）。

## 可再優化項目（建議下一步）

1. 代碼風格一致化
	- 目標：讓 `uv run ruff check .` 全綠。
	- 建議：先 `ruff --fix`，再人工處理長行與訊息文案斷行。

2. 設定治理
	- 將高風險設定（外部 API key 相關）集中在 README 與 `.env.example` 一致描述。
	- 在啟動時列出關鍵功能開關狀態，降低部署誤設機率。

3. 文件治理
	- 保留「開發計畫」文件作歷史記錄，另加註狀態（archive / design note）。
	- 未來功能變更時，README 與 docs 同步更新，避免規格落差。

## 檔案刪除判斷

### 可安全刪除（本機產物）

1. `MagicMock/`
2. `.pytest_cache/`
3. `.ruff_cache/`

以上已處理或可隨時重建，不影響功能。

### 不建議直接刪除（目前仍有用途）

1. `scripts/*.py`：維運與資料流程仍使用中。
2. `docs/LINE本地模型聊天機器人_開發計畫.md`：仍可作為設計背景文件。
3. `linebot_clean_onefile.spec`：打包必需。

