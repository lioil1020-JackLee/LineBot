可以，而且我建議你直接做一次減法式重構，不要在現有 BotService 上繼續疊 patch。

你現在這個專案的主要問題不是單點 bug，而是「功能太多、路由太雜、責任混在一起」。app.py 同時初始化 DB、LLM、RAG、memory、task、fact-check、health、webhook，已經是過胖的 composition root 。BotService 也同時處理能力宣告、時效判斷、market/weather、knowledge、agent loop、長期記憶、task、response pacing、query complexity、guard、fallback，規則與 prompt 常數非常多，還保留圖片/檔案/程式碼相關能力文字，複雜度明顯過高

你的目標其實很明確：

保留 LINE 文字對話
保留知識庫優先
知識庫沒有答案時，自動上網查
回答要像「博士型萬事通」
刪掉圖片、檔案、code 讀寫等你不想要的功能

這種情況最適合的，不是「修一修」，而是做一個新核心、舊殼保留的修改計畫。

一、重構目標

新版本只保留這四條主線：

LINE 文字訊息入口
知識庫檢索回答
網路搜尋後整合回答
對話記憶與基本安全護欄

其他全部視為非核心，能刪就刪。

你應該把系統從現在的：

多功能助理平台

改成：

文字型研究助理 / 通才型問答 bot

二、你要刪掉的功能

依 repo 現況，這些都建議移除或停用。

1. 圖片 OCR / 檔案解析能力

BotService 的系統 prompt 仍明寫支援圖片 OCR 與 PDF/DOCX/XLSX/PPTX/TXT 檔案解析
這跟你的新方向相反，應整段刪掉。

要刪的範圍：

ImageOCRService
DocumentParserService
bot.py 內 image/file event 分支
任何 blob download / OCR / parser fallback 訊息
prompt 內「可讀圖片/檔案」描述
2. coding assistance / code call / 讀寫 code 痕跡

BotService.__init__ 還保留 coding_assistance_enabled 參數，reply normalization 也有清理 <code-call> 等標記，代表系統曾經朝 code/tool agent 方向擴充過
既然你明確不要讀寫 code，這條應整段清掉。

要刪的範圍：

coding_assistance_enabled
guard.coding_blocked
code/tool call cleanup 相關模板
agent loop 中任何 code-oriented path
capability reply 裡與 code 有關的描述
3. market / weather 特例路由

你不想要一堆 rule，那 weather / market 的 domain-specific route 其實也該降級。現在 BotService 內建 WeatherService、MarketService 與多條 grounded route
新架構應改成：

都先經過 planner
planner 決定是否要 search
weather/market 不再是寫死的特殊大分支

也就是說，不一定要全刪 service，但至少不該再當主架構核心。

4. task/profile memory 的產品化功能

app.py 目前還掛了 /admin/session/.../profile、/tasks 等管理端點，且初始化了 ProfileMemoryService、TaskMemoryService、SessionTaskRepository
如果你目標是「博士萬事通」，這些不是第一優先，先砍能讓核心變乾淨。

5. agent loop 與過度 prompt 模板

BotService 裡有 _AGENT_AUTO_SEARCH、_AGENT_MAX_TOOL_ROUNDS、run_agent_loop 匯入，還有大量「回答節奏 / 題型補強 / 深度 / followup / continuity」prompt 片段
這些不是不能存在，而是目前量太多，已經壓過真正重要的「證據規劃」。建議大砍，只留最少量 style prompt。

三、你要保留的功能

只保留這些：

1. Session 與訊息儲存

這是多輪對話基礎，應保留。

SessionRepository
MessageRepository
SessionService

app.py 裡這些目前就是核心依賴之一

2. LLMService

保留，但責任只剩：

生成回答
生成 planner JSON
生成 memory summary

不要再把一堆路由判斷塞進 LLMService 外圍 prompt。

3. RAGService / KnowledgeRepository

這是你的「本地知識庫優先」基礎，應保留。app.py 已經有 KnowledgeRepository 與 RAGService 組裝

4. WebSearchService

這是你要讓 bot 聰明的關鍵。不是拿來做快速 fallback，而是升級成正式二階段回答流程的一部分。現在 BotService 已有 _search_web_results() 與 WebSearchService.from_settings(...) 的路徑可延用

5. ResponseGuardService

保留，但改任務。它不再負責一堆風格修飾，而是專注做：

沒證據時禁止時效性斷言
禁止硬編日期、價格、比分、新聞事實
證據不足時改寫成保守回答
四、新架構藍圖

你要把核心改成下面這個流程：

LINE text
→ BotController
→ ResearchPlannerService
→ KnowledgeFirstAnswerService
→ WebResearchService
→ AnswerComposerService
→ ResponseGuardService
→ LINE reply

1. BotController

只做 orchestration，不做業務判斷。

責任：

收訊息
讀 session/context
呼叫 planner
根據 plan 呼叫 knowledge / web research
存訊息
回覆
2. ResearchPlannerService

這是整個重構的核心。

輸出結構化 plan，例如：

needs_external_info
prefer_knowledge_base
freshness
query_intent
search_queries
forbid_unverified_claims

它不直接回答問題，只決定「怎麼找答案」。

3. KnowledgeFirstAnswerService

先查本地知識庫：

若命中高信心內容，直接整理回答
若不足，回傳 insufficient_evidence
4. WebResearchService

當 knowledge 不足時執行：

query rewrite
multi-query search
result filtering
freshness check
answerability check
5. AnswerComposerService

根據 evidence 寫出最後答案：

有本地知識 → 用本地知識回答
無本地知識但有 web evidence → 整理後回答
都不足 → 誠實說不知道，並說明缺口
6. ResponseGuardService

最後一道關：

偵測高風險斷言
若 plan 要求外部證據但 evidence 不足，禁止確定語氣
若內容疑似 hallucination，轉保守語氣
五、建議的刪改順序
Phase 1：瘦身

先刪不需要的功能，不先追求「更聰明」。

刪除項目
圖片 / 檔案事件處理
OCR / parser service
coding assistance
agent loop
task/profile memory
market/weather 專用 route
capability prompt 中圖片/檔案/code 敘述
過度細分的 response pacing / intent prompt 常數
保留項目
LINE text webhook
session/message repo
llm service
rag service
web search service
response guard
health / llm logs（可保留）

這一步的成果是：專案從多功能平台，縮成單純文字研究 bot。

Phase 2：改 routing

把現在 BotService 裡的 rule-heavy 流程拆掉。

你現在的 BotService 充滿：

_looks_realtime_sensitive_query
_build_fast_fallback_reply
_build_grounded_realtime_reply
_looks_weather_query
_looks_market_query
_build_grounded_general_lookup_reply
_build_unified_knowledge_reply

這種 function 很多，表示 routing 是「功能疊功能」

改成只剩三條 route：

knowledge_direct
search_then_answer
direct_reasoning

其中：

direct_reasoning 只限 timeless/general 問題
其他只要牽涉 today/latest/current/facts，就一定要 evidence-first
Phase 3：導入 planner

新增 ResearchPlannerService。

輸出 schema 建議：

class ResearchPlan(BaseModel):
    route: Literal["knowledge_direct", "search_then_answer", "direct_reasoning"]
    needs_external_info: bool
    needs_knowledge_base: bool
    freshness: Literal["none", "recent", "today", "realtime"]
    search_queries: list[str]
    forbid_unverified_claims: bool
    answer_style: Literal["concise", "balanced", "deep"]

這層要靠：

少量 heuristics
LLM planner prompt

而不是全靠關鍵字。

Phase 4：升級 web search

現在 repo 的 web search 用法偏「搜一下然後拼摘要」。新版本要升級成研究流程。

至少要做：

query rewrite
多 query
trusted domain filter
freshness scoring
duplicate removal
result ranking
answerability check

例如使用者問：
「今天有甚麼棒球賽」

planner 產生：

今日 棒球 賽程
CPBL 今日賽程
MLB schedule today
NPB schedule today

而不是只搜原句。

Phase 5：重寫回答生成

回答策略要改成：

A. 知識庫夠

「根據知識庫，答案是……」

B. 網路查到

「我查到目前可靠來源顯示……」

C. 證據不足

「我目前沒有查到足夠可靠的資料，因此不能直接下結論。」

絕對不要再出現：
「今天沒有棒球賽」
這種沒證據的否定句。

六、建議的檔案重構
保留並重寫
src/linebot_app/app.py
src/linebot_app/bot.py
src/linebot_app/services/bot_service.py

但 bot_service.py 最好拆掉，不要再留成巨型 service。

新增
services/research_planner_service.py
services/knowledge_first_service.py
services/web_research_service.py
services/answer_composer_service.py
刪除或停用
services/image_ocr_service.py
services/document_parser_service.py
agent_loop.py
與 coding/tool call 相關 service/module
不再需要的 market/weather special route module
task/profile memory 相關 service 與 admin route
七、app.py 修改計畫

現在 app.py 初始化了太多服務
新版本應縮成：

settings
db init
session repo / message repo / llm log repo / knowledge repo
llm service
prompt service
rag service
web search service
response guard service
planner service
knowledge first service
web research service
answer composer service
bot controller

管理端點也應刪掉一大半，只留：

/health
/health/detail
/admin/knowledge/reindex
/admin/llm-logs
/webhook

/profile、/tasks、/model set 這類非必要先刪。

八、bot.py 修改計畫

bot.py 只保留：

簽章驗證
text event 處理
群組 mention 判斷
將文字丟給 bot controller
reply text

刪掉：

image event
file event
OCR / parser 下載與轉文字
對「可讀檔案」的能力回覆
九、BotService 的替代設計

你可以把原本 BotService 拆成這樣：

ChatOrchestrator
取得 session/context
執行 plan
整合 evidence
存 user/assistant messages
ResearchPlannerService
判斷是否需要 search
產生 query
KnowledgeFirstService
查 rag
若命中，產生 grounded answer
WebResearchService
搜尋、過濾、評分、整理 evidence
AnswerComposerService
最後寫出符合風格的答案
ResponseGuardService
高風險內容修正

這樣單一元件才可測。

十、刪舊代碼原則

你說得很對，舊代碼不能只「先留著」。這種專案最怕半死不活的 dead code。

建議原則：

直接刪，不註解保留

凡是確定不要的功能，直接移除，不要留 deprecated_ 或 legacy_。

先跑一輪全域搜尋

找出這些關鍵字：

image
file
ocr
parser
docx
pdf
xlsx
pptx
coding
code_call
tool_call
agent_loop
market
weather
task
profile

把引用點全部列出，再決定是刪 module 還是改 import。

刪 prompt 常數

BotService 現在很多常數只是為了舊流程存在。這類 dead prompt 最該清。像 _RUNTIME_CAPABILITY_PROMPT 就應重寫，不該再提圖片與檔案

十一、你要的「像博士萬事通」要怎麼做

這不是靠把 prompt 寫得很像專家，而是靠三件事：

1. 先判斷何時不能亂答

也就是 planner + guard。

2. 查到資料後要會整合

不是只貼連結，而是：

給結論
補理由
說明限制
標示來源性質
3. 知識庫與網路要有優先順序

優先順序建議：

先查本地知識庫
不足再查網路
若網路結果仍不足，誠實說不足

這才像可靠的研究助理，而不是話很多的 bot。

十二、實作優先順序

我建議你這樣分 4 週期做：

第 1 週：瘦身版上線
刪圖片/檔案/code/task/profile/agent
保留文字對話 + rag + web search
第 2 週：planner 上線
新增 ResearchPlannerService
改成 evidence-first routing
第 3 週：web research 強化
query rewrite
freshness / trust / answerability check
第 4 週：品質收尾
guard
prompt 縮編
回答風格統一
測試補齊
十三、驗收標準

改完後至少要通過這些情境：

類型 A：知識型

「什麼是鋼骨結構」
→ 不查網路也能答得好

類型 B：知識庫型

「我們知識庫裡關於某主題怎麼寫」
→ 優先引用本地知識庫

類型 C：即時型

「今天有什麼棒球賽」
→ 自動上網查，不可憑空回答

類型 D：查不到

「某個很冷門的即時資訊」
→ 說查不到可靠資料，不亂編

類型 E：多輪型

「那要怎麼開始」
→ 能接續前文，但不靠大量脆弱規則

十四、最務實的結論

你的方向應該是：

把這個 repo 從「功能很多但不夠穩的 bot 平台」，重構成「文字型知識研究助理」。

核心做法不是再補更多規則，而是：

大砍非核心功能 + 引入 planner + 知識庫優先 + 網路研究後整合 + 無證據不斷言。

如果你要，我下一步可以直接幫你把這份計畫再往下落成兩種格式之一：

工程待辦清單版：按檔案與模組列出刪改項目
實作藍圖版：直接幫你寫出新的目錄結構、service 介面與 BotService 替代骨架