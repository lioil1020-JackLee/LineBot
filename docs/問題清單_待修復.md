# 問題清單與修復狀態

更新日期：2026-04-09

## 狀態摘要

- P0 已完成：搜尋層改為單一穩定來源策略，不再走 `ddgs` / MCP 多重備援。
- P0 已完成：`agent_loop` 已改為以當前原始問題為主，避免上一題污染 `web_search`。
- P0 已完成：核心 service、主要測試與文件已清掉亂碼與舊殘留。
- P1 已完成：舊 persona repository、外部 LLM 備援、MCP gateway、未使用 prompt 檔已移除。
- P1 進行中：持續把剩餘腳本與大檔模組整理到可維護狀態，並補齊 lint 與文件。

## 已完成項目

### 搜尋與即時資料
- 搜尋鏈只保留單一來源策略，不再串接多套備援。
- `weather_service` 以 Open-Meteo 為主。
- `market_service` 以 TWSE MIS API 為主，並補上買賣盤價格回退。
- `VW T-ROC R 2024版 性能規格如何` 已走固定可驗證來源。
- 一般知識查詢已可走通用 grounded 路由；若只查到社群型來源，會明確標示而不直接偽裝成高可信答案。

### 對話與路由
- `agent_loop` 已補 hard reset 與 raw query 優先策略。
- `你的底層模型是什麼` 不再被前一題股票脈絡污染。
- 自我介紹、OCR、檔案解析、response guard、factcheck 相關模組已改成可讀文字與乾淨 prompt。

### 清理與刪除
- 已刪除舊 `embedded_mcp_gateway.py`
- 已刪除舊 `mcp_client_service.py`
- 已刪除舊 `external_llm_service.py`
- 已刪除舊 `persona_repository.py`
- 已刪除未使用 `factcheck_prompts.py`
- 已刪除舊 smoke 腳本與不再需要的相容設定
- 已把天氣 / 股價 / 車規的 grounded reply 邏輯自 `bot_service.py` 抽離成獨立 service，降低主流程耦合

## 持續整理項目

- 大檔模組持續收斂風格與長行，讓 `ruff check .` 全綠。
- 文件同步更新，避免狀態落後於實作。
- 若再發現確定未使用的檔案或匯出，繼續移除。

## 驗證案例

- `台北 天氣怎麼樣`
- `今天台積電股價是多少`
- `神達股價` -> `你的底層模型是什麼`
- `VW T-ROC R 2024版 性能規格如何`
